pipeline {
    agent any 
stages {
        stage('Build') { 
steps {
                echo "Building done here" }
        }
        stage('Test') { steps {
                echo "Testing done here" }
        }
        stage('Deploy') { steps {
                echo "Deploying done here" }
        }
    }
}
